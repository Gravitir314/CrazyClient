package io.decagames.rotmg.ui.popups.signals 
{
    import org.osflash.signals.*;
    
    public class ClosePopupByClassSignal extends org.osflash.signals.Signal
    {
        public function ClosePopupByClassSignal()
        {
            super(Class);
            return;
        }
    }
}
